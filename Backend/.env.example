# Backend environment variables example
# Copy this to .env and fill the values before running

# Server
PORT=3000
NODE_ENV=development
JWT_SECRET=your_jwt_secret_here

# Postgres (fill these from your environment / secrets manager)
DB_HOST=your_db_host
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name
DB_SSL=true

# Note: replace DB_* values with your actual credentials in your local .env (do NOT commit it)
